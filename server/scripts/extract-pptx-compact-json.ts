// Retained as a compatibility entry point. PowerPoint bytes are read from stdin and the complete
// compact presentation JSON is returned on stdout; no input, output, or asset paths are created.
await import('./parse-pptx')

export {}
